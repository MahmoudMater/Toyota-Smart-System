# Logs page overrides

- Sticky header with filter controls and integration tabs
- JetBrains Mono for log body
- Color-coded left borders: request (blue), response (green), error (red), retry (amber), event (purple)
- Connect to middleware `/logs` namespace (not Next.js origin)

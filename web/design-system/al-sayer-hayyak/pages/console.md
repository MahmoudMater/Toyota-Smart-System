# Console page overrides

- Full HudNav at top
- Three-column grid for avatar / session / queue on large screens
- StatusDot for socket connection
- Queue badges use semantic colors (waiting, notified, confirmed, skipped)
- Claim countdown in monospace accent color
